# Auto detect text files and perform LF normalization
* text=auto

# Shell 脚本与 gradlew 必须保持 LF，否则 Linux 上无法执行
*.sh text eol=lf
gradlew text eol=lf

# Windows 批处理保持 CRLF
*.bat text eol=crlf

# 二进制文件不做任何转换
*.jar binary
*.png binary
*.zip binary
